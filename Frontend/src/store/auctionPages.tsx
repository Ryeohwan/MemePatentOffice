import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const auctionPageSlice = createSlice({
    name: "auctionPage",
    initialState: initialState,
    reducers: {
    },
  });